package com.xjeffrose.gordo.server;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class GordoServer {
  private static final Logger log = LoggerFactory.getLogger(GordoServer.class.getName());

  private GordoConfig config;

  public GordoServer(GordoConfig config) {
    this.config = config;
  }

  public void start() {

  }
}
